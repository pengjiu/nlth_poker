# Contract Kit

Single-schema source + code generation for:

- validator (strict null / controlled enums / missing fields)
- canonicalizer (ARCHIETECTURE.md §3.5.1 + SeatMap exception §3.5.1.1)
- hasher (sha256 lowercase hex, 64 chars)

Current scope: **Python-only** generated outputs are used by runtime/tools.
